apiVersion: v1
kind: Service
metadata:
  name: {{ include "llamafactory.fullname" . }}
  labels:
    {{- include "llamafactory.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "llamafactory.selectorLabels" . | nindent 4 }}
